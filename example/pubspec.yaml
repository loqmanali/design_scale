name: design_scale_example
description: Example application for the design_scale package.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  design_scale:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.2 <7.0.0"

flutter:
  uses-material-design: true
