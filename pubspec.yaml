name: design_scale
description: Reference-based design scaling for Flutter with uniform viewport scaling and native accessibility preservation.
version: 0.1.0-dev.3
homepage: https://github.com/loqmanali/design_scale
repository: https://github.com/loqmanali/design_scale
issue_tracker: https://github.com/loqmanali/design_scale/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.2 <7.0.0"

topics:
  - flutter
  - responsive
  - scaling
  - design-system
  - adaptive-ui
